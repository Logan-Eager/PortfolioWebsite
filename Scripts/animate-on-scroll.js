// This script is designed to animate any elements that are apart of the
// "animate-on-scroll" class when they are scrolled into view. It uses the
// Intersection Observer API to detect when the elements are in the viewport and applies
// a CSS class to trigger the animation.

// Helpful snippets
// const footer = document.querySelector("footer");
// footer.classList.remove("fade-in");
// document.addEventListener("scroll", handleScroll, { passive: true });


